/**
 * Use this when mapping user data from an api response.
 */
export interface User {
  username: string,
  firstName: string,
  lastName: string,
  fullName: string,
  email: string,
  status: number,
  dateCreated: string
}
